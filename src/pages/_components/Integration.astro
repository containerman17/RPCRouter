---
import IntegrationCode from "./IntegrationCode";
import Btn from "../../components/Btn";
---

<div class="overflow-hidden bg-white py-24 sm:py-32">
    <div class="mx-auto max-w-7xl md:px-6 lg:px-8">
        <div class="grid grid-cols-1 gap-x-8 gap-y-16 sm:gap-y-20 lg:grid-cols-2 lg:items-start">
            <div class="px-6 md:px-0 lg:pr-4 lg:pt-4">
                <div class="mx-auto max-w-2xl lg:mx-0 lg:max-w-lg">
                    <h2 class="text-base font-semibold leading-7 text-primary-600">Plug & Play</h2>
                    <p class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Already compatible with your Web3 App</p>
                    <p class="mt-6 text-lg leading-8 text-gray-600">RPCRouter works with all popular Web3 libraries. Just switch the endpoint—no code changes needed.</p>
                    {
                        /* <dl class="mt-10 max-w-xl space-y-8 text-base leading-7 text-gray-600 lg:max-w-none">
                                {features.map((feature) => (
                                    <div key={feature.name} class="relative pl-9">
                                        <dt class="inline font-semibold text-gray-900">
                                            <feature.icon class="absolute left-1 top-1 h-5 w-5 text-primary-600" aria-hidden="true" />
                                            {feature.name}
                                        </dt>{' '}
                                        <dd class="inline">{feature.description}</dd>
                                    </div>
                                ))}
                            </dl> */
                    }
                    <div class="mt-4">
                        <Btn to="/login" className="mr-2">Get your RPC URL</Btn>
                        <Btn type="secondary" to="/docs/">Read docs</Btn>
                    </div>
                </div>
            </div>
            <div class="sm:px-6 lg:px-0">
                <IntegrationCode client:visible />
            </div>
        </div>
    </div>
</div>
